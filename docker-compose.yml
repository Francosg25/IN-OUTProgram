services:
  validator-service:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data # Para guardar reportes de errores
      
    deploy:
      resources:
        limits:
          memory: 2G # Evita que el proceso consuma toda la RAM del server
    restart: always